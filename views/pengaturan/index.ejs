<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Pengaturan KA Lab</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <style>
    * {
      box-sizing: border-box;
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      margin: 0;
      padding: 0;
    }

    body {
      background: linear-gradient(to right, #e0eafc, #cfdef3);
      display: flex;
      justify-content: center;
      align-items: center;
      min-height: 100vh;
      padding: 20px;
    }

    .card {
      width: 400px;
      padding: 2rem;
      border-radius: 20px;
      background: #ffffff;
      box-shadow: 0 20px 40px rgba(0, 0, 0, 0.1);
      transition: all 0.3s ease;
    }

    .card:hover {
      transform: scale(1.02);
      box-shadow: 0 25px 45px rgba(0, 0, 0, 0.15);
    }

    .card h2 {
      margin-bottom: 1rem;
      font-size: 24px;
      text-align: center;
      color: #333;
    }

    .info-group {
      margin-bottom: 1rem;
    }

    .info-label {
      font-weight: bold;
      color: #555;
      display: block;
      margin-bottom: 4px;
    }

    .info-value {
      color: #222;
      padding: 8px 12px;
      border-radius: 8px;
      background-color: #f6f6f6;
      border: 1px solid #e0e0e0;
    }

    .footer {
      margin-top: 2rem;
      text-align: center;
      font-size: 13px;
      color: #888;
    }

    .back-btn {
      display: inline-block;
      margin-top: 1.5rem;
      padding: 10px 16px;
      background-color: #1e88e5;
      color: white;
      border: none;
      border-radius: 8px;
      text-decoration: none;
      font-weight: bold;
      transition: background 0.3s;
    }

    .back-btn:hover {
      background-color: #1565c0;
    }
  </style>
</head>
<body>

  <div class="card">
    <h2>Pengaturan KA Lab</h2>

    <div class="info-group">
      <span class="info-label">Nama</span>
      <div class="info-value"><%= user.nama %></div>
    </div>

    <div class="info-group">
      <span class="info-label">Email</span>
      <div class="info-value"><%= user.email %></div>
    </div>

    <div class="info-group">
      <span class="info-label">Username</span>
      <div class="info-value"><%= user.username %></div>
    </div>

    <div class="info-group">
      <span class="info-label">Laboratorium</span>
      <div class="info-value"><%= user.lab %></div>
    </div>

    <div class="info-group">
      <span class="info-label">Jumlah Pengguna</span>
      <div class="info-value"><%= user.jumlahPengguna %> orang</div>
    </div>

    <div class="info-group">
      <span class="info-label">Versi Aplikasi</span>
      <div class="info-value"><%= user.versi %></div>
    </div>

    <div class="info-group">
      <span class="info-label">Terakhir Update</span>
      <div class="info-value"><%= user.terakhirUpdate %></div>
    </div>

    <div class="footer">
      <a href="/dashboard" class="back-btn">Kembali ke Dashboard</a>
    </div>
  </div>

</body>
</html>
